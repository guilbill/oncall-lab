<!-- Copyright 2026 Anthropic PBC -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# STACK.md — capability → connection map

<!-- Generated by oncall-setup Phase 0 (Discover). This is the ONLY file in
     the kit that names vendors. Skills refer to capabilities; this file
     binds them. Regenerate when connections change. -->

Generated: {{date}} · Channel: {{#channel}} · Probed by: oncall-setup

| Capability | Bound to | Probe result | Notes |
|---|---|---|---|
| `metrics` | {{e.g. the Datadog connection}} | {{✅ listed dashboards; read ci.* metrics}} | {{key dashboards by name}} |
| `logs` | {{e.g. the Splunk connection}} | {{✅ ran a test query}} | {{index names}} |
| `pager` | {{e.g. the PagerDuty connection}} | {{✅ read last 5 incidents; NOT tested: creating pages}} | {{service + escalation policy names}} |
| `code` | {{e.g. the GitHub connection}} | {{✅ read CODEOWNERS, recent PRs}} | {{repos in scope}} |
| `alert-channels` | {{#channels, listed}} | {{✅ member of all}} | {{which are automated-only vs. chatter}} |
| `incidents` | {{where incident records live: threads in the on-call channel (default) / per-incident channels / pager incidents / tickets}} | {{✅ can read + post to records}} | {{how a human opens one, e.g. "🔴 INC:" thread}} |
| `deploys` | {{feed/channel, or "via code"}} | {{…}} | {{…}} |
| `flags` | {{e.g. the feature-flag tool}} | {{✅ read flag states + change history; NOT tested: writes (none permitted)}} | {{flag changes are timeline events for triage; ramp-downs are proposed as plans and executed by humans}} |

## Gaps

<!-- Anything unbound, and what degrades without it. Examples: -->
- {{e.g. no `pager` connected → paging routine skipped; page-severity
  incidents fall back to @-mentioning {{@escalation}} per ONCALL.md}}

## Access posture

All bindings are read-only, always. The kit does not support write
credentials to monitored systems; the only outbound action a binding may
carry is sending a page via the `pager` capability. The `flags` binding is
read-only like all others — ramp proposals are plans a human executes. Sole permitted
exception: if `ONCALL.md` records the opt-in alert-editor extension, the
`metrics`/alerting binding may carry an **additive-only** rule-creation
credential ({{not opted in / opted in on {{date}}, credential: {{name}}}}).
