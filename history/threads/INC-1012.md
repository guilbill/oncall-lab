# INC-1012 - Lint passes locally, fails in CI

Channel: #ci-alerts | Opened 2026-09-01 09:00 UTC | sev3

**09:00  ci-bot**
> :rotating_light: CI failed on main
> Repo guilbill/oncall-lab | Commit `1012abc` | <run logs>

**09:06  guilbill**
Lint is green on my machine and red in CI on the same commit.

**09:15  guilbill**
Path resolution. Fixed, but I have only seen this once so I am not sure it covers every case.

**09:25  guilbill**
Resolved. Resolved the src path from the module URL instead of the working directory.
